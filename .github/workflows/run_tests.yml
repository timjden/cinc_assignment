name: Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: python -m pip install -r requirements.txt
      - run: ls
      - run: cd features
      - run: pwd
      - run: behave -v
      - run: pwd
